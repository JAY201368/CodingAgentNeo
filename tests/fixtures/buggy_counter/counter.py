"""Intentionally defective increment helper used by AC-01 drills."""


def increment(value: int) -> int:
    """Return ``value + 1``. The body is wrong on purpose until the agent fixes it."""

    return value + 2
