"""Agent transport bindings.

Concrete adapters depend on the frontend-independent ``AgentBackend`` port;
the package intentionally does not assemble a backend at import time.
"""

__all__: list[str] = []
